#include <stdio.h>
int main(){
    int tab[5], i, max;
    printf("Podaj 5 elementow tablicy\n");
    for(i = 0; i < 5; i++)
        scanf("%d", &tab[i]);
        max = tab[0];
    for(i = 1; i < 5; i++)
        if(max < tab[i]) max = tab[i];
            printf("Max= %d\n", max);
return 0;
}
